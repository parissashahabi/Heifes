// import clientApi from "../../../../services/interceptor";

export const getCartItemsCount = (setCartItemCount: any) => {
  // clientApi.cartItems.cartItemControllerGetCartItemCount().then(res => {
  //   //update cartItems count in header
  //   setCartItemCount(res.data.totalCount);
  // }).catch(err => {
  //   console.log(err);
  // });
};
